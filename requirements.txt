# NeuroLearn — Backend Requirements
# =====================================
# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Google AI (Gemini agents)
google-generativeai>=0.7.0

# Numerical
numpy>=1.26.0

# ML — optional but strongly recommended
# PyTorch (LSTM mastery model)
# Install: pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch>=2.2.0

# NLP semantic answer validation (optional)
# pip install sentence-transformers
sentence-transformers>=2.6.0

# Eye tracking
mediapipe>=0.10.0
opencv-python>=4.9.0

# Spaced repetition / scheduling (built-in, no extra deps)
# SM-2 implemented in app.py

# Testing
pytest>=8.0.0
httpx>=0.27.0   # for FastAPI test client
